# 部署到沒有域名的伺服器 (使用 IP 訪問)
# 例如: http://你的伺服器IP
:80 {
    # 壓縮
    encode zstd gzip

    # 前端靜態文件代理
    handle /* {
        reverse_proxy frontend:80
    }

    # 後端 API 代理 (加上 /api 前綴的請求轉發給 Spring Boot)
    handle /api/* {
        reverse_proxy backend:8080
    }
}
